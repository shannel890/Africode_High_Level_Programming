# AND. True when both operands are true

#is_student = False
#is_present = True
#print(is_student and is_present)

# OR. True when at least one operand is true

#is_student = True
#is_present = True
#print(is_student or is_present)

# NOT. True when operand is false - it negates the operand
#print(not is_student) # False
#print(not is_present)
#def find_lowest_value(numbers):
    #if not numbers:
        #return None
    #return min(numbers)
#numbers = [ 2, 3, 4, 5]
#print(find_lowest_value(numbers))
#a = 5
#a /= 2
#print(a)


# Addition of list items
#num1 = 10
#num2 = 20
#add = num1 + num2
#print(add)

#a=input("Enter the number 1 :")
#b=input("Enter the number 2 :")
#sum=int(a)+int(b)
#print ("addition is :",sum)

#num = [1,2,3,4,5]
#print(sum(num))

#def find_lowest_value(numbers):
    #if not numbers:
        #return "List is empty"
    #return min(numbers)

# Example usage
#numbers = [10, 3, 7, 1, 9]
#print(find_lowest_value(numbers))

#def find_highest_value(numbers):
    #if not numbers:
        #return "List is empty"
    #return max(numbers)

# Example usage
#numbers = [10, 3, 7, 1, 9]
#print("Highest value:", find_highest_value(numbers))


#sum = 0
#for i in list:
    #sum = sum + i
    #print(sum)

#def find_duplicates(lst):
    #seen = set()
    #duplicates = set()

    #for item in lst:
        #if item in seen:
            #duplicates.add(item)
        #else:
            #seen.add(item)

    #return duplicates

# Example usage
numbers = [20,87,87,90,43,98,87,95]
sum = 0
for i in numbers:
    sum = sum + i
    print(sum)

#duplicates = find_duplicates(numbers)

#if duplicates:
    #print("Repeated values:", duplicates)
# else:
#     print("No repeated values found.")
